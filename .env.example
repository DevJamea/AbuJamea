# PostgreSQL connection string (used by both the app and Drizzle CLI)
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/app_db

# Public-facing application URL (used for links and callbacks)
NEXT_PUBLIC_APP_URL=http://localhost:3000
